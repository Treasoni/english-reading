interface:
  display_name: "Maintain Learnings"
  short_description: "Audit repeated learning errors"
  default_prompt: "Use $maintain-learnings to audit .learnings, fix recurring skill issues, and archive resolved records."
